# readIQ app configuration
# Copy this file to .env. The proxy URL is safe to ship in Expo; the OpenAI key
# belongs only in Vercel's Environment Variables and must never appear here.

EXPO_PUBLIC_AI_PROXY_URL=https://readiq-openai-proxy.vercel.app
