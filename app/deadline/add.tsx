import { useRouter } from 'expo-router'; import { useState } from 'react'; import { ScrollView, Text, TextInput } from 'react-native'; import { Button, Eyebrow } from '../../components/ui'; import { useTheme } from '../../hooks/use-theme';
export default function AddDeadline() { const { colors } = useTheme(); const router = useRouter(); const [title, setTitle] = useState(''); return <ScrollView contentInsetAdjustmentBehavior="automatic" style={{ backgroundColor: colors.bg }} contentContainerStyle={{ padding: 20, gap: 18 }}><Eyebrow>Plan ahead</Eyebrow><Text style={{ color: colors.text, fontSize: 30, fontWeight: '900' }}>Add a deadline</Text><TextInput value={title} onChangeText={setTitle} placeholder="What is due?" placeholderTextColor={colors.muted} style={{ backgroundColor: colors.card, borderColor: colors.border, borderWidth: 1, borderRadius: 16, padding: 16, color: colors.text, fontSize: 17 }} /><TextInput placeholder="Due date · e.g. May 30" placeholderTextColor={colors.muted} style={{ backgroundColor: colors.card, borderColor: colors.border, borderWidth: 1, borderRadius: 16, padding: 16, color: colors.text, fontSize: 17 }} /><Button title="Save reminder" onPress={() => router.replace('/deadlines')} /></ScrollView>; }
