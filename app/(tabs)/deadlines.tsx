import { useRouter } from 'expo-router';
import { ScrollView, Text, View } from 'react-native';
import { Card, Eyebrow, Button } from '../../components/ui';
import { deadlines } from '../../data/demo';
import { useTheme } from '../../hooks/use-theme';
export default function Deadlines() { const { colors } = useTheme(); const router = useRouter(); return <ScrollView contentInsetAdjustmentBehavior="automatic" style={{ backgroundColor: colors.bg }} contentContainerStyle={{ padding: 20, paddingBottom: 110, gap: 18 }}><Eyebrow>Stay ahead</Eyebrow><Text style={{ color: colors.text, fontSize: 30, fontWeight: '900' }}>Deadlines</Text><Button title="＋ Add reminder" onPress={() => router.push('/deadline/add')} />{deadlines.map((item) => <Card key={item.id} style={{ gap: 10 }}><View style={{ flexDirection: 'row', justifyContent: 'space-between' }}><Text style={{ color: colors.text, fontSize: 19, fontWeight: '800' }}>{item.title}</Text><Text style={{ color: item.urgency === 'this week' ? colors.danger : colors.warning, fontWeight: '800' }}>{item.days} days</Text></View><Text style={{ color: colors.muted }}>{item.subject} · Due {item.date}</Text><View style={{ height: 7, borderRadius: 5, backgroundColor: colors.border }}><View style={{ width: item.urgency === 'this week' ? '72%' : '30%', height: 7, borderRadius: 5, backgroundColor: item.urgency === 'this week' ? colors.danger : colors.warning }} /></View><Text style={{ color: colors.muted, fontSize: 13 }}>◷ Reminder on</Text></Card>)}</ScrollView>; }
