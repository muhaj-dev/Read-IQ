import { useRouter } from 'expo-router';
import { ScrollView, Text, View } from 'react-native';
import { Card, Eyebrow, Button } from '../../components/ui';
import { notes } from '../../data/demo';
import { useTheme } from '../../hooks/use-theme';
export default function Quiz() { const { colors } = useTheme(); const router = useRouter(); return <ScrollView contentInsetAdjustmentBehavior="automatic" style={{ backgroundColor: colors.bg }} contentContainerStyle={{ padding: 20, paddingBottom: 110, gap: 18 }}><Eyebrow>Turn memory into practice</Eyebrow><Text style={{ color: colors.text, fontSize: 30, fontWeight: '900' }}>Build a better recall loop.</Text><Text style={{ color: colors.muted, fontSize: 16 }}>Every question is generated from a saved note and carries its source.</Text><Card style={{ gap: 16 }}><Text style={{ color: colors.text, fontSize: 20, fontWeight: '800' }}>Choose a format</Text><View style={{ flexDirection: 'row', gap: 12 }}><Card style={{ flex: 1, backgroundColor: colors.accentSoft, borderColor: colors.accent }}><Text style={{ fontSize: 26 }}>▣</Text><Text style={{ color: colors.text, fontWeight: '800' }}>Flashcards</Text><Text style={{ color: colors.muted, fontSize: 12 }}>Active recall</Text></Card><Card style={{ flex: 1 }}><Text style={{ fontSize: 26 }}>◉</Text><Text style={{ color: colors.text, fontWeight: '800' }}>Multiple choice</Text><Text style={{ color: colors.muted, fontSize: 12 }}>Test yourself</Text></Card></View><Text style={{ color: colors.text, fontWeight: '800' }}>Study from</Text>{notes.slice(0, 2).map((note) => <View key={note.id} style={{ flexDirection: 'row', justifyContent: 'space-between', paddingVertical: 7 }}><Text style={{ color: colors.text }}>{note.title}</Text><Text style={{ color: colors.success }}>✓ selected</Text></View>)}<Button title="Generate 5 questions" onPress={() => router.push('/quiz/active')} /></Card></ScrollView>; }
